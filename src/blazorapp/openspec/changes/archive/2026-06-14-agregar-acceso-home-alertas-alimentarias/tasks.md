## 1. Acceso desde Home

- [x] 1.1 Ajustar `HomeDashboard.razor` para sustituir el acceso ligado a `home_card_activities_title` por un enlace a `Alertas Alimentarias`.
- [x] 1.2 Hacer que el enlace navegue a la página principal existente de `Alertas Alimentarias`.

## 2. Composición visual y textos

- [x] 2.1 Mostrar el icono `bi bi-basket2` a la izquierda y el título a la derecha dentro del acceso de Home.
- [x] 2.2 Añadir o reutilizar los literales necesarios para que el acceso muestre `Alertas Alimentarias` en lugar de `Actividades`.
- [x] 2.3 Añadir el ajuste SCSS mínimo necesario para que la composición horizontal del acceso quede integrada en Home sin estilos inline.

## 3. Validación

- [x] 3.1 Verificar manual o automáticamente que `HomeDashboard` muestra el acceso correcto y que ya no se renderiza `home_card_activities_title` como tarjeta visible.
- [x] 3.2 Verificar la navegación desde Home hasta `/alertas-alimentarias` y la presencia del icono a la izquierda del título.
